# Detta är en platshållare för favicon.ico
# 
# I en verklig implementation skulle denna fil vara en faktisk .ico-fil
# med binärt bildinnehåll. För att lägga till en riktig favicon,
# ersätt den här filen med en faktisk .ico-fil eller tillhandahåll
# en alternativ lösning. 